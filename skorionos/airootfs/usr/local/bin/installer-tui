#!/usr/bin/env python3
"""SkorionOS text installer (Textual) entry point."""
import sys

sys.path.insert(0, "/usr/local/lib")

from installer.tui_main import main

if __name__ == "__main__":
    sys.exit(main())
